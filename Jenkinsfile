pipeline {
    agent any
    stages {
        stage('Build the program in Java') {
            steps {
                echo 'Build'
            }
		}
		stage('Test the program in Java') {
            steps {
                echo 'Test'
            }
		}
		stage('Deploy the program') {
            steps {
                echo 'Deploy'
            }	
        }
    }
}
