pipeline {
    agent any
    stages {
        stage('Build the program') {
            steps {
                echo 'Build'
            }
		}
		stage('Test the program') {
            steps {
                echo 'Test'
            }
		}
		stage('Deploy the program') {
            steps {
                echo 'Deploy'
            }	
        }
    }
}
